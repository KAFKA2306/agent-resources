# Autonomous merge proof

This file is a low-risk canary for the factory merge controller.

A pull request changing this file must pass exact-head validation and deployment evidence before the machine merge executor may merge it. The canary exists only to prove the normal path requires no human merge action.
