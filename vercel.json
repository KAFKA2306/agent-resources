{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "(cd docs && uvx --from mkdocs-material mkdocs build) && mkdir -p deploy/site && cp -r docs/dashboard deploy/dashboard && cp -r docs/site/* deploy/site/",
  "outputDirectory": "deploy",
  "ignoreCommand": "test -n \"$VERCEL_GIT_PREVIOUS_SHA\" && git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- docs api dashboard vercel.json",
  "functions": {
    "api/dashboard-live.js": {
      "maxDuration": 60
    }
  },
  "redirects": [
    {
      "source": "/",
      "destination": "/dashboard/",
      "permanent": false
    }
  ],
  "rewrites": [
    {
      "source": "/dashboard/dashboard.json",
      "destination": "/api/dashboard-live"
    }
  ],
  "headers": [
    {
      "source": "/dashboard/dashboard.json",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store, max-age=0"
        }
      ]
    }
  ]
}
